@model IEnumerable<_420_476.Projet.Session.Models.Service>

@{
    ViewBag.Title = "Index";
}

<script type="text/javascript">

    function openMessage(offrantID, userID) {
        var offrant = offrantID;
        var user = userID;

        $.ajax({
            type: "POST",
            url: "/Products/getProductDetails",
            data: form,
            datatype: "html",
            success: function (data) {
                alert("success");
                $("#ajaxResult").html(data);
            }
        })
    }

</script>

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<div>
    @Html.ActionLink("Precedent", "PreviousServices", "Navigation");
    @Html.ActionLink("Suivant", "NextServices", "Navigation");
</div>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Label)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Prix)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Offrant.Region)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Label)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Prix)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Offrant.Region)
        </td>
        <td>
            <a onclick="openMessage(@item.OffrantID +' '+@Session["UserID"])" style="cursor:pointer;">Envoyer Message</a>
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>*@
    </tr>
}

</table>
